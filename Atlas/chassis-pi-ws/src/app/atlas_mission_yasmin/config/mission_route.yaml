mission:
  navigation_backend: nav2_competition
  manipulation_backend: vision_arm

  # A/B 半图和实际坐标由导航后端维护；YASMIN 只使用语义点位
  waypoints:
    pickup:
      id: pickup
      timeout_s: 30.0
    park_1:
      id: park_1
      timeout_s: 30.0
    park_2:
      id: park_2
      timeout_s: 30.0
